using tkKey = OpenTK.Input.Key;

namespace XogoEngine.Input
{
    public enum Key
    {
        Left = tkKey.Left,
        Right = tkKey.Right,
        Up = tkKey.Up,
        Down = tkKey.Down,
        Space = tkKey.Space
    }
}
